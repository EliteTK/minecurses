#include "minesweeper.h"
#include <stdio.h>
#include <stdlib.h>
#include <getopt.h>

void main_loop()
{
	bool running = true;

	while (running) {
		/* get input */
		/* parse input */
		/* redraw */
	}
}

int main(int argc, char **argv)
{
	/* parse args */
	/* initialise everything */
	/* enter main loop */
	return EXIT_SUCCESS;
}
